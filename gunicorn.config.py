workers = 5
# the amount of server space/cores that we have available to our server
# worker node is an actual server
bind = "0.0.0.0:8080"
# will bind to any IP address and then open port 8080(where most internet traffic comes to.)